[package]
name = "argus-frontier"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["sync", "time"] }
anyhow = "1"
url = "2"
argus-common = { path = "../argus-common" }
